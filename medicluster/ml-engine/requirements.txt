flask==3.0.3
flask-cors==4.0.1
pandas==2.2.1
numpy==1.26.4
scikit-learn==1.4.1.post1
scipy==1.12.0
gunicorn==21.2.0

# Deep learning core
# CPU-only: pip install torch==2.2.1 torchvision==0.17.1 --index-url https://download.pytorch.org/whl/cpu
torch==2.2.1
torchvision==0.17.1

# Medical imaging models (chest X-ray pathology detection)
torchxrayvision==1.0.1
Pillow==10.2.0

# DICOM file support (real hospital files)
pydicom

# Image preprocessing improvements
albumentations

# Fast CPU inference
onnxruntime

# ── ML Explainability ──────────────────────────────────────────────────────────
shap>=0.45.0
xgboost>=2.0.0

# ── Dimensionality Reduction (UMAP / t-SNE) ───────────────────────────────────
umap-learn>=0.5.6
# t-SNE is bundled in scikit-learn (no extra install needed)

# ── NLP / Clinical NER ────────────────────────────────────────────────────────
# Transformers (for BioBERT, ClinicalBERT, summarization, sentiment)
transformers>=4.40.0
# spaCy base (required by scispacy)
spacy>=3.7.0
# scispaCy for biomedical NER (optional but recommended)
# pip install scispacy && python -m spacy download en_core_sci_md
scispacy>=0.5.5

# ── Time-Series Forecasting ───────────────────────────────────────────────────
# Prophet for trend decomposition and anomaly detection
prophet>=1.1.5

# ── Vector Store / RAG Chatbot ────────────────────────────────────────────────
faiss-cpu>=1.8.0
langchain>=0.2.0

# ── Grad-CAM Visualization ────────────────────────────────────────────────────
# pytorch-grad-cam is used as reference; we implement natively using hooks
# Uncomment if you prefer the library version:
# pytorch-grad-cam>=1.5.0
